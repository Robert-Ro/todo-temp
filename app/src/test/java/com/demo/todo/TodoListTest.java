package com.demo.todo;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class TodoListTest {

    @Test
    public void testAddItem() {
        // TodoList todoList = new TodoList();
        // ToDoFactory toDo = new ToDoFactory(0, "title1", "description1", false);
        // todoList.addItem(toDo);

        // assertEquals(1, todoList.getSize());
        // assertTrue(todoList.containsItem(toDo));
    }

    @Test
    public void testAddRemove() {
        // TodoList list = new TodoList();

        // ToDoFactory todo1 = new ToDoFactory(1, "Title 1", "Description 1", false);
        // ToDoFactory todo2 = new ToDoFactory(2, "Title 2", "Description 2", false);

        // list.addItem(todo1);
        // assertEquals(1, list.getSize());
        // assertTrue(list.containsItem(todo1));
        // assertFalse(list.containsItem(todo2));

        // list.addItem(todo2);
        // assertEquals(2, list.getSize());
        // assertTrue(list.containsItem(todo1));
        // assertTrue(list.containsItem(todo2));

        // list.removeItem(todo1);
        // assertEquals(1, list.getSize());
        // assertFalse(list.containsItem(todo1));
        // assertTrue(list.containsItem(todo2));

        // list.removeItem(todo2);
        // assertEquals(0, list.getSize());
        // assertFalse(list.containsItem(todo1));
        // assertFalse(list.containsItem(todo2));

    }

    @Test
    public void testClearList() {
        // TodoList list = new TodoList();

        // ToDoFactory todo1 = new ToDoFactory(1, "Title 1", "Description 1", false);
        // ToDoFactory todo2 = new ToDoFactory(2, "Title 2", "Description 2", false);

        // list.addItem(todo1);
        // list.addItem(todo2);
        // assertEquals(2, list.getSize());

        // list.clearList();
        // assertEquals(0, list.getSize());
        // assertFalse(list.containsItem(todo1));
        // assertFalse(list.containsItem(todo2));
    }
}
